package com.posgrado.gui;

public class MenuPrincipal {
  /*private final Stage stage;
  private String directorioSeleccionado;

  public MenuPrincipal(Stage stage) {
    this.stage = stage;
  }

  private MenuItem hacerAbrirMenuItem() {
    MenuItem abrir = new MenuItem("Abrir");
    configurarAbrir(abrir);
    return abrir;
  }

  private configurarAbrir(MenuItem abrir) {
    abrir.setOnAction(new EventHandler<ActionEvent>() {
      public void handle(ActionEvent t) {

      }
    });
  }

  private class AbrirMenuItem extends MenuItem {
    AbrirMenuItem(Stage stage) {
      super("Abrir");
      configurarEvento(this, stage);
    }

    private static void configurarEvento(MenuItem item, final Stage st) {
      item.setOnAction(new EventHandler<ActionEvent>() {
        public void handle(ActionEvent t) {
          DirectoryChooser selectorDirectorioInstancias = hacerSelectorDirectorio(new File("."));
          File directorioSeleccionado = selectorDirectorioInstancias.showDialog(st);
        }
      });
    }


    private static DirectoryChooser hacerSelectorDirectorio(File carpetaInicial) {
      DirectoryChooser selectorDirectorio = new DirectoryChooser();
      selectorDirectorio.setTitle("Seleccionar instancias");
      selectorDirectorio.setInitialDirectory(carpetaInicial);
      return selectorDirectorio;
    }
  }*/
}
